import type { Architecture } from '../types/domain';

export const ARCHITECTURES: Architecture[] = [
  {
    id: 'ocgan_v1',
    name: 'OCGAN baseline (v1/v2)',
    shortName: 'OCGAN v1',
    date: '2026-04-15',
    family: 'OCGAN',
    status: 'deprecated',
    macroAUROC: 0.7866,
    inferenceMs: 320,
    trainTimeSec: 450,
    bankSizeMB: 153,
    paramsMillions: 38.5,
    architecture_type: 'GAN-based reconstruction + multi-head fusion',
    core_idea:
      'Frozen ResNet → encoder → decoder reconstruction. The anomaly score is a weighted fusion of seven heads: reconstruction MSE, perceptual loss, feature discrepancy, latent compactness, memory bank, teacher-student distillation, and a discriminative head.',
    strengths: [
      'End-to-end trainable',
      'Multi-component fusion theoretically robust',
      'Reconstruction-based heatmaps via diff map',
    ],
    weaknesses: [
      '3 architectural flags dead in code (use_skip_connections, scoring_topk, unfreeze_from)',
      'AUROC inflated by lucky val splits (val_mixed_ratio=0.5)',
      'GAN training expensive (~30s/cat × 5 seeds × 15 cats)',
      'Trained encoder weaker than frozen ImageNet features',
    ],
    improvements: ['Initial baseline'],
    components: [
      'reconstruction',
      'memory_bank',
      'teacher_student',
      'latent_compactness',
      'score_fusion_weighted',
      'score_fusion_learned',
      'synthetic_anomalies',
    ],
    hyperparameters: {
      backbone: 'resnet50',
      latent_dim: 256,
      memory_max_patches: 1024,
      memory_layer: 'layer3',
      train_epochs: 30,
      batch_size: 8,
      learning_rate: 1e-4,
      score_weights: 'recon=0.3, perceptual=0.2, feature=0.2, latent=0.1, teacher_student=0.1, memory=0.05, discriminative=0.05',
    },
    pipeline: [
      { label: 'Input', detail: '256×256 RGB image', type: 'input' },
      { label: 'Backbone (ResNet50)', detail: 'partially frozen, layer3+layer4 features', type: 'process' },
      { label: 'Encoder', detail: 'project features → latent (256-dim)', type: 'process' },
      { label: 'Decoder (Residual)', detail: 'reconstruct image from latent', type: 'process' },
      { label: 'Memory Bank (1k)', detail: 'k-center on layer3', type: 'storage' },
      { label: 'Teacher-Student', detail: 'frozen teacher vs trainable student', type: 'process' },
      { label: 'Score Fusion', detail: 'weighted sum of 7 normalised scores', type: 'process' },
      { label: 'Anomaly Score', detail: 'fused output + threshold', type: 'output' },
    ],
    files: [
      'models/components.py',
      'models/backbones/resnet_backbone.py',
      'trainers/base_trainer.py',
      'configs/default.yaml',
    ],
    available_for_inference: true,
    notes:
      'Three configuration flags (use_skip_connections, scoring_topk, unfreeze_from) were declared but never wired to the actual code paths.',
  },
  {
    id: 'ocgan_v3',
    name: 'OCGAN v3 (Sprint 1 fixes)',
    shortName: 'OCGAN v3',
    date: '2026-04-19',
    family: 'OCGAN',
    status: 'deprecated',
    macroAUROC: 0.7584,
    inferenceMs: 280,
    trainTimeSec: 480,
    bankSizeMB: 153,
    paramsMillions: 42.1,
    architecture_type: 'GAN reconstruction + UNet skip connections + selective unfreeze',
    core_idea:
      'Same v2 pipeline but with the three dead flags actually wired: UNet reconstructor receives encoder skips, layer3 unfrozen with separate optimizer group, top-k pooling activated on residual scores. Adds deterministic Perlin synthetic anomalies.',
    strengths: [
      'Architecturally correct',
      'Determinism via item_seed = seed*1e6 + epoch*1e5 + idx',
      'Top-k pooling reduces score variance',
      'Vectorised Perlin noise generator',
    ],
    weaknesses: [
      'AUROC dropped vs v2 (more honest val splits exposed weaknesses)',
      'NaN instability in val_normal scoring (capsule, screw, pill, metal_nut, cable)',
      'EMA swap at val time triggers numerical issues',
    ],
    improvements: [
      'use_skip_connections wired (UNetReconstructor)',
      'scoring_topk wired (top-k pooling on residuals)',
      'unfreeze_from=layer3 wired',
      'Single backbone pass (no double inference)',
      'val_mixed_ratio 0.5 → 0.25',
      'deterministic=true',
    ],
    components: [
      'reconstruction',
      'memory_bank',
      'teacher_student',
      'latent_compactness',
      'score_fusion_weighted',
      'score_fusion_learned',
      'synthetic_anomalies',
      'topk_aggregation',
    ],
    hyperparameters: {
      backbone: 'resnet50',
      reconstructor: 'unet_skip',
      latent_dim: 256,
      scoring_topk: 100,
      unfreeze_from: 'layer3',
      memory_max_patches: 1024,
      synthetic_mode: 'perlin',
      val_mixed_ratio: 0.25,
      ema_decay: 0.999,
    },
    pipeline: [
      { label: 'Input + Perlin', detail: 'augmentation with Perlin synthetic anomalies', type: 'input' },
      { label: 'Backbone (partial unfreeze)', detail: 'layer3 trainable, two AdamW groups', type: 'process' },
      { label: 'UNet Reconstructor', detail: 'with skip connections from layer1..4', type: 'process' },
      { label: 'Memory Bank', detail: 'k-center coreset', type: 'storage' },
      { label: 'Top-K Pooling', detail: 'k=100 on per-pixel residuals', type: 'process' },
      { label: 'Score Fusion (learned)', detail: 'logistic regression on val_mixed', type: 'process' },
      { label: 'Anomaly Score', detail: 'EMA swap at validation', type: 'output' },
    ],
    files: [
      'models/reconstruction/unet_reconstructor.py',
      'configs/default_mvtec.yaml',
      'utils/perlin.py',
      'trainers/base_trainer.py',
    ],
    available_for_inference: true,
    delta_vs_previous: -0.0282,
    notes: 'The drop is not architectural regression but more honest evaluation.',
  },
  {
    id: 'patchcore_v1',
    name: 'PatchCore-pure v1',
    shortName: 'PatchCore v1',
    date: '2026-05-02',
    family: 'PatchCore',
    status: 'deprecated',
    macroAUROC: 0.9051,
    inferenceMs: 90,
    trainTimeSec: 7,
    bankSizeMB: 60,
    paramsMillions: 68.9,
    architecture_type: 'Frozen backbone + memory bank (no training)',
    core_idea:
      'wide_resnet50_2 frozen → layer2+layer3 features pooled and concatenated → k-center coreset of 10k patches → top-k=3 mean nearest-neighbour distance. NO GAN, NO reconstruction, NO fusion. Just feature matching.',
    strengths: [
      'No training (only feature extraction)',
      '+12pp jump vs OCGAN v3',
      'Fully deterministic (init=mean k-center)',
      'Fast (~7s/cat for full pipeline)',
      'Production-grade reference implementation',
    ],
    weaknesses: [
      'Coreset 10k drops 80% of patches',
      'Pill/zipper/screw/capsule still weak',
      'top-k=3 mean too aggressive',
    ],
    improvements: [
      'Stripped all GAN machinery',
      'Frozen ImageNet > trained encoder',
      'K-center greedy coreset',
      'Multi-scale layer2+layer3 concat',
    ],
    components: ['frozen_backbone', 'memory_bank', 'kcenter_coreset', 'multi_scale', 'topk_aggregation'],
    hyperparameters: {
      backbone: 'wide_resnet50_2',
      feature_level: 'layer2+layer3',
      aggregation: 'topk_mean',
      topk: 3,
      coreset: 10000,
      candidate_pool: 20000,
      image_size: 256,
    },
    pipeline: [
      { label: 'Input', detail: '256×256 RGB, ImageNet normalised', type: 'input' },
      { label: 'wide_resnet50_2 (frozen)', detail: 'no training, ImageNet weights', type: 'process' },
      { label: 'Multi-scale features', detail: 'pool layer2 → layer3 spatial size, concat', type: 'process' },
      { label: 'K-center coreset (10k)', detail: 'greedy diverse subsampling', type: 'storage' },
      { label: 'min L2 distance', detail: 'each patch → nearest in bank', type: 'process' },
      { label: 'top-k=3 mean', detail: 'aggregate per-patch distances', type: 'process' },
      { label: 'Anomaly Score', detail: 'image-level scalar', type: 'output' },
    ],
    files: ['scripts/patchcore_pure.py'],
    available_for_inference: false,
    delta_vs_previous: 0.1467,
    notes: 'Big idea: frozen ImageNet features beat any trained encoder for one-class anomaly detection.',
  },
  {
    id: 'patchcore_v2',
    name: 'PatchCore v2 (topk_reweighted)',
    shortName: 'PatchCore v2',
    date: '2026-05-02',
    family: 'PatchCore',
    status: 'deprecated',
    macroAUROC: 0.9397,
    inferenceMs: 90,
    trainTimeSec: 7,
    bankSizeMB: 60,
    paramsMillions: 68.9,
    architecture_type: 'Frozen backbone + memory bank + reweighted aggregation',
    core_idea:
      'Same as v1 but with topk_reweighted aggregation (k=9) instead of topk_mean (k=3). Reweighting downplays redundant top-k distances via softmax of inverse distances.',
    strengths: [
      '+3.5pp vs v1',
      'Better aggregation that down-weights redundant patches',
      'Same speed as v1 (no extra cost)',
    ],
    weaknesses: [
      'Coreset 10k still undersized',
      'Sensitive to softmax temperature',
    ],
    improvements: [
      'topk_reweighted: weights = 1 - softmax(1/distance)',
      'Heavier weight on truly large distances',
      'k increased from 3 → 9',
    ],
    components: ['frozen_backbone', 'memory_bank', 'kcenter_coreset', 'multi_scale', 'topk_aggregation'],
    hyperparameters: {
      backbone: 'wide_resnet50_2',
      feature_level: 'layer2+layer3',
      aggregation: 'topk_reweighted',
      topk: 9,
      coreset: 10000,
      candidate_pool: 20000,
    },
    pipeline: [
      { label: 'Input', detail: '256×256 RGB, ImageNet normalised', type: 'input' },
      { label: 'wide_resnet50_2 (frozen)', detail: '', type: 'process' },
      { label: 'Multi-scale features', detail: 'layer2+layer3 concat', type: 'process' },
      { label: 'K-center coreset (10k)', detail: '', type: 'storage' },
      { label: 'min L2 distance', detail: 'per patch', type: 'process' },
      { label: 'top-k=9 reweighted', detail: 'w = 1 - softmax(1/d), aggregate', type: 'process' },
      { label: 'Anomaly Score', detail: 'image-level scalar', type: 'output' },
    ],
    files: ['scripts/patchcore_pure.py', 'scripts/run_patchcore_v2.sh'],
    available_for_inference: false,
    delta_vs_previous: 0.0346,
  },
  {
    id: 'patchcore_v3',
    name: 'PatchCore v3 (full bank)',
    shortName: 'PatchCore v3',
    date: '2026-05-03',
    family: 'PatchCore',
    status: 'deprecated',
    macroAUROC: 0.9828,
    inferenceMs: 105,
    trainTimeSec: 10,
    bankSizeMB: 380,
    paramsMillions: 68.9,
    architecture_type: 'Frozen backbone + full memory bank',
    core_idea:
      'Same as v2 but max_patches=70000 → bypasses k-center coreset for 14/15 categories → full bank retains all patches. Dramatic boost on texture-heavy categories like zipper (0.72→0.98) and capsule (0.77→0.98).',
    strengths: [
      '+4.3pp vs v2 (massive)',
      'Maximum patch information retention',
      'No coreset bottleneck',
    ],
    weaknesses: [
      'Bank size 280-500MB per category',
      'K-center still needed for hazelnut (80k raw patches)',
      '~5GB total disk for all 15 banks',
    ],
    improvements: [
      'max_patches=70000 keeps full bank',
      'Zipper +25.8pp, capsule +20.5pp',
      'Threshold calibration on held-out val_normal (15%)',
    ],
    components: ['frozen_backbone', 'memory_bank', 'multi_scale', 'topk_aggregation', 'threshold_calibration'],
    hyperparameters: {
      backbone: 'wide_resnet50_2',
      feature_level: 'layer2+layer3',
      aggregation: 'topk_reweighted',
      topk: 9,
      max_patches: 70000,
      val_normal_ratio: 0.15,
      threshold_method: 'p99 of val_normal scores',
    },
    pipeline: [
      { label: 'Input', detail: '256×256 RGB, ImageNet normalised', type: 'input' },
      { label: 'wide_resnet50_2 (frozen)', detail: '', type: 'process' },
      { label: 'Multi-scale features', detail: 'layer2+layer3 concat', type: 'process' },
      { label: 'Full bank (70k cap)', detail: 'no coreset for 14/15 cats', type: 'storage' },
      { label: 'min L2 distance', detail: '', type: 'process' },
      { label: 'top-k=9 reweighted', detail: '', type: 'process' },
      { label: 'Threshold (p99 val_normal)', detail: 'held-out 15% calibration', type: 'process' },
      { label: 'Anomaly Score + Heatmap', detail: 'spatial heatmap from per-patch min-dists', type: 'output' },
    ],
    files: ['scripts/patchcore_pure.py', 'scripts/run_patchcore_v3.sh', 'logs/patchcore_v3.csv'],
    available_for_inference: false,
    delta_vs_previous: 0.0431,
  },
  {
    id: 'patchcore_p1',
    name: 'PatchCore + layer1 (screw-only)',
    shortName: 'PatchCore L1+L2+L3',
    date: '2026-05-04',
    family: 'PatchCore',
    status: 'experimental',
    macroAUROC: 0.9478,
    inferenceMs: 115,
    trainTimeSec: 12,
    bankSizeMB: 499,
    paramsMillions: 68.9,
    architecture_type: 'Frozen backbone + 3-level multi-scale + full bank',
    core_idea:
      'For screw only: add layer1 to the multi-scale concatenation (layer1+layer2+layer3 pooled to layer3 spatial size). The lowest level captures fine thread textures that layer2 misses.',
    strengths: [
      'Screw +2.7pp (0.9147 → 0.9419)',
      'Bank dimensionality 1792 (vs 1536 standard)',
      'Same simple pipeline',
    ],
    weaknesses: [
      'Only screw benefits (tested grid/pill/toothbrush, no gain or marginal)',
      'Larger bank size due to higher dim',
    ],
    improvements: ['layer1 captures fine textures'],
    components: ['frozen_backbone', 'memory_bank', 'multi_scale', 'topk_aggregation', 'threshold_calibration'],
    hyperparameters: {
      backbone: 'wide_resnet50_2',
      feature_level: 'layer1+layer2+layer3',
      aggregation: 'topk_reweighted',
      topk: 9,
      max_patches: 70000,
      bank_dim: 1792,
    },
    pipeline: [
      { label: 'Input', detail: '256×256 RGB', type: 'input' },
      { label: 'wide_resnet50_2 (frozen)', detail: '', type: 'process' },
      { label: '3-level concat', detail: 'layer1+layer2+layer3 pooled to L3 grid', type: 'process' },
      { label: 'Full bank', detail: '70k cap', type: 'storage' },
      { label: 'top-k=9 reweighted', detail: '', type: 'process' },
      { label: 'Score', detail: '', type: 'output' },
    ],
    files: ['scripts/patchcore_pure.py', 'scripts/run_p1_tuning.sh', 'logs/patchcore_p1.csv'],
    available_for_inference: false,
    delta_vs_previous: -0.035,
    notes: 'Treated as a per-category override in the production model rather than a separate architecture.',
  },
  {
    id: 'production_final',
    name: 'PatchCore Production (final)',
    shortName: 'Production',
    date: '2026-05-04',
    family: 'PatchCore',
    status: 'production',
    macroAUROC: 0.9846,
    inferenceMs: 100,
    trainTimeSec: 10,
    bankSizeMB: 380,
    paramsMillions: 68.9,
    architecture_type: 'Per-category optimal config',
    core_idea:
      'Production deployment with per-category feature_level overrides. 14 categories use layer2+layer3, screw uses layer1+layer2+layer3. All with topk_reweighted/k=9, max_patches=70000, threshold p99 val_normal. Deployed via FastAPI server.',
    strengths: [
      'Macro AUROC 0.9846 (+19.8pp vs OCGAN baseline)',
      'Production-ready REST API',
      'Lazy loading of category models',
      '~100ms inference (warm)',
      'Spatial heatmap from per-patch distances',
    ],
    weaknesses: [
      'Bank total ~5GB on disk',
      'First request slow (~490ms backbone+bank load)',
    ],
    improvements: [
      'Per-category tuning (screw uses 3 levels)',
      'FastAPI inference server',
      'Bank export pipeline',
      'Robust threshold calibration',
    ],
    components: [
      'frozen_backbone',
      'memory_bank',
      'multi_scale',
      'topk_aggregation',
      'threshold_calibration',
    ],
    hyperparameters: {
      backbone: 'wide_resnet50_2',
      feature_level_default: 'layer2+layer3',
      feature_level_screw: 'layer1+layer2+layer3',
      aggregation: 'topk_reweighted',
      topk: 9,
      max_patches: 70000,
      threshold_method: 'p99 of val_normal hold-out (15%)',
    },
    pipeline: [
      { label: 'Image upload', detail: 'POST /api/predict (multipart)', type: 'input' },
      { label: 'Lazy model load', detail: 'first request loads bank from disk', type: 'process' },
      { label: 'Backbone forward', detail: 'wide_resnet50_2 frozen', type: 'process' },
      { label: 'Per-cat features', detail: 'layer2+layer3 (default) or +layer1 (screw)', type: 'process' },
      { label: 'Bank lookup', detail: 'cdist → min per patch', type: 'process' },
      { label: 'top-k=9 reweighted', detail: '', type: 'process' },
      { label: 'JSON response', detail: 'score, heatmap, prob, components', type: 'output' },
    ],
    files: [
      'server.py',
      'models/patchcore_inference.py',
      'scripts/export_patchcore_banks.py',
      'production_models/{cat}/patchcore_bank.pt',
    ],
    available_for_inference: true,
    delta_vs_previous: 0.0018,
    notes: 'The current deployed model. Macro AUROC matches PatchCore v3 plus screw P1 boost.',
  },
];

export const ARCH_BY_ID: Record<string, Architecture> = Object.fromEntries(
  ARCHITECTURES.map((a) => [a.id, a])
);

export const ARCH_FAMILIES = ['OCGAN', 'PatchCore', 'Hybrid'] as const;

export const STATUS_COLORS: Record<string, string> = {
  baseline: 'bg-slate-500',
  deprecated: 'bg-amber-500',
  experimental: 'bg-violet-500',
  production: 'bg-emerald-500',
};
