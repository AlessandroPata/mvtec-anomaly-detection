# OCGAN2026 — core dependencies, pinned for reproducibility (verified on Python 3.13).
#
# torch + torchvision are installed SEPARATELY with the CUDA build — the +cuXXX
# local builds are not on PyPI, so they can't be pinned here. Install with:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cu128
#   (CPU-only:  pip install torch torchvision)
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
scikit-learn==1.9.0
matplotlib==3.11.0
pillow==12.2.0
pyyaml==6.0.3
tqdm==4.68.2
albumentations==2.0.8
opencv-python==4.13.0.92
timm==1.0.27
einops==0.8.2
omegaconf==2.3.1
hydra-core==1.3.3
wandb==0.27.2
lpips==0.1.4
scikit-image==0.26.0
jupyterlab==4.5.8
ipykernel==7.3.0
pytest==9.0.3
rich==15.0.0
pytorch-msssim==1.0.0
